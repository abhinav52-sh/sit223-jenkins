version: "3.8"

services:
  your-app-name:
    build: .
    ports:
      - "3000:3000" # Map host port 3000 to container port 3000 (adjust as needed)
    volumes:
      - .:/app # Mount your project directory into the container

# Add other services for your application (database, etc.) if needed
